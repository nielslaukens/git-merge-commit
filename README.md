git-merge-commit - find the first merge commit containing the given commits
===========================================================================

git-merge-commit tries to find the first (i.e. oldest) commit that contains
(i.e. has as ascestor) all listed commits.
